services:
  - type: web
    name: labour-wage-management-system
    runtime: node
    buildCommand: npm ci --omit=dev && npm rebuild sqlite3
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
      - key: NODE_ENV
        value: production
